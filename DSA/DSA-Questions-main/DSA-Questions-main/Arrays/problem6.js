const veg=["Potato", "Tomato", "Egg Plant", "Cucumber"];


// replace the last vegetable with a fruit named apple

veg[veg.length-1]="Apple";
console.log(veg);

const data = [1]
data[data.length-1] = 2
console.log(data)